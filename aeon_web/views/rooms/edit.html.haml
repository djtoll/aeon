%p
  ==#{@room.name} (#{@room.object_id} - #{@room.id})
  

=form_for :room, :action => url(:room, @room) do
  %p=text_field :name
  %p=text_area :description, :rows => 10, :cols => 60
  
  %p=submit "Submit"